🚨 SOLUCIÓN DEFINITIVA: Deshabilitar Reconexión Automática
============================================================

El problema es que Baileys se reconecta automáticamente cada vez que
se cierra la conexión, creando un bucle infinito.

✅ SOLUCIÓN INMEDIATA:

Deshabilitar la reconexión automática en baileys-stable-service.ts

Busca esta línea (alrededor de línea 230):

```typescript
const shouldReconnect = (lastDisconnect?.error as Boom)?.output?.statusCode !== DisconnectReason.loggedOut
```

CÁMBIALA POR:

```typescript
const shouldReconnect = false // 🔒 DESHABILITADO: Evitar bucle infinito
```

Esto detendrá el bucle inmediatamente.

📋 PASOS:

1. Abre: src/lib/baileys-stable-service.ts
2. Busca la línea 230 (aproximadamente)
3. Cambia shouldReconnect a false
4. Guarda el archivo
5. Reinicia el servidor

El sistema de auto-conexión (cada 30 segundos) se encargará de
reconectar si es necesario, pero sin crear bucles infinitos.

---
URGENTE: Aplica este cambio AHORA para detener el bucle
